---
description: Design Tokens을 Figma에 적용하는 워크플로우 (Variables, Text Styles, Components)
---

# 디자인 토큰 → Figma 변환 워크플로우

## 개요
프로젝트의 디자인 토큰(colors, typography, spacing 등)을 Figma Variables, Text Styles, Components로 자동 생성합니다.

---

## 사전 준비

### 1. 토큰 파일 구조 확인
프로젝트에 다음 파일들이 있는지 확인:
- `design-tokens/tokens/colors.json` - 컬러 팔레트
- `design-tokens/tokens/typography.json` - 타이포그래피
- `design-tokens/tokens/spacing.json` - 간격
- `design-tokens/tokens/radius.json` - 모서리 반경

또는 `tailwind.config.js`에서 토큰 정보 확인

### 2. 필요한 폰트 설치
- **Manrope** (영문): [Google Fonts](https://fonts.google.com/specimen/Manrope)
- **Pretendard** (한글): [GitHub](https://github.com/orioncactus/pretendard)

---

## 방법 1: Figma 플러그인 UI 사용 (권장)

### 1. 플러그인 로드
```
Figma → Plugins → Development → Import plugin from manifest
→ figma-plugin/manifest.json 선택
```

### 2. Import UI 실행
```
Plugins → Design Tokens Importer → 📁 Import Tokens (UI)
```

### 3. 파일 업로드
- colors.json, typography.json 등 드래그앤드롭
- 생성할 항목 선택 (Variables / Text Styles / Components)
- "Figma에 생성하기" 클릭

---

## 방법 2: 기본 토큰으로 바로 생성

플러그인에 하드코딩된 기본 토큰(neobank 프로젝트 기준)으로 생성:

```
Plugins → Design Tokens Importer → 📦 Create All (Default)
```

또는 개별 실행:
- 🎨 Create Variables - Colors, Spacing, Radius Variables 생성
- 📝 Create Text Styles - Typography Text Styles 생성
- 🧩 Create Components - Button, Input, Card 등 컴포넌트 생성

---

## 새 프로젝트에 적용하기

### 1. 플러그인 복사
```bash
cp -r {현재프로젝트}/design-tokens/figma-plugin {새프로젝트}/design-tokens/figma-plugin
```

### 2. 토큰 파일 수정
`main.js`의 `tokens` 객체를 새 프로젝트에 맞게 수정:

```javascript
var tokens = {
    colors: {
        primary: {
            "100": "#새색상",
            "500": "#메인색상",
            // ...
        }
    },
    typography: {
        "h1": { fontSize: 48, lineHeight: 56, fontWeight: 600 },
        // ...
    }
};
```

### 3. 또는 UI로 JSON 파일 불러오기
Import UI를 통해 새 프로젝트의 토큰 JSON 파일을 직접 업로드

---

## 생성되는 항목

### Variables (Figma 변수)
| Collection | 변수 |
|------------|------|
| Neobank Colors | primary/100, primary/500, status/error 등 |
| Neobank Spacing | spacing/0, spacing/4, spacing/8 등 |
| Neobank Radius | radius/sm, radius/lg, radius/full 등 |
| Neobank Typography | typo/h1/fontSize, typo/l3/fontWeight 등 |

### Text Styles
- typo-h1, typo-h1-ko
- typo-l2, typo-l3
- typo-b1, typo-b2
- 등 17개 스타일

### Components
- Button / Default / sm, md, lg
- Button / Secondary, Disabled
- Input / Default, Focus, Error
- Card
- Badge / Default, Success, Error
- Avatar / sm, md, lg
- Progress, Separator, Skeleton

---

## 트러블슈팅

### 폰트 로드 실패
- Manrope/Pretendard 폰트가 시스템에 설치되어 있는지 확인
- Fallback으로 Roboto 사용됨

### Variables 중복 에러
- 기존 Variables 삭제 후 다시 실행
- Figma → Local Variables → 컬렉션 삭제

### Text Styles 미적용
- 컴포넌트 생성 전에 Text Styles가 먼저 생성되어야 함
- "Create All" 사용 권장
