---
description: Git 작업 브랜치 커밋 (master 사용 금지, 푸쉬는 요청 시에만)
---

# Git 커밋 워크플로우

> ⚠️ **master 브랜치는 절대 사용하지 않습니다!**

## 사전 확인

// turbo
1. 현재 브랜치 확인
```bash
git branch --show-current
```

**master면** → 새 브랜치 생성 필수!

> ⚠️ **작업자 이름을 모르면 먼저 물어보기!**
> "브랜치에 사용할 이름 또는 이니셜을 알려주세요 (예: jinwoo, jw)"

```bash
git checkout -b feature/작업자-작업이름-YYYYMMDD
```

> 💡 브랜치명 예시: `feature/jinwoo-design-token-20260130`, `fix/jw-readme-update-20260130`

---

## 커밋 단계

// turbo
2. 변경 내용 확인
```bash
git status
git diff --stat
```

### 2-1. 메타데이터 확인 (`.agent/` 폴더 파일만)

> ⚠️ **skills, rules, workflows 파일에는 반드시 작성자 정보 필요!**

변경된 파일 중 `.agent/skills/`, `.agent/rules/`, `.agent/workflows/` 에 있는 `.md` 파일이 있으면:

1. YAML frontmatter에 아래 필드가 있는지 확인:
   - `author`: 최초 작성자
   - `modified_by`: 마지막 수정자
   - `created`: 최초 작성일 (YYYY-MM-DD)
   - `modified`: 마지막 수정일 (YYYY-MM-DD)

2. **없으면 반드시 추가!** (작업자 이름 모르면 물어보기)

```yaml
---
name: 파일명
description: 설명
author: 작업자이름        # ← 필수
modified_by: 작업자이름   # ← 필수
created: 2026-01-30       # ← 필수
modified: 2026-01-30      # ← 필수
---
```

3. 변경 내용을 사용자에게 요약 보고
- 수정된 파일 목록
- 주요 변경 사항 간단 설명

4. 사용자 확인 후 커밋
```bash
git add -A
git commit -m "커밋 메시지"
```

> 💡 커밋 메시지는 한국어로, 변경 내용을 명확하게 작성

---

## 푸쉬 (요청 시에만!)

> 기본적으로 **커밋만** 수행합니다.
> 사용자가 "푸쉬해줘" 또는 "PR 만들어줘" 요청 시에만 아래 진행.

// turbo
5. 원격에 푸쉬
```bash
git push origin $(git branch --show-current)
```

6. master로 **새 Pull Request 생성**
```bash
gh pr create --base master --head $(git branch --show-current) --title "PR 제목" --body "변경 사항 설명"
```

> ⚠️ **매 작업마다 새 브랜치 + 새 PR!** (기존 PR에 누적 금지)

---

## 안전 규칙

| 규칙 | 설명 |
|------|------|
| ❌ master 커밋 금지 | 절대 master에 직접 커밋하지 않음 |
| ❌ master 푸쉬 금지 | 절대 master에 직접 푸쉬하지 않음 |
| ❌ 기존 PR 누적 금지 | 새 작업은 새 브랜치에서 시작 |
| ✅ 작업별 브랜치 | `feature/작업명-날짜` 형식 |
| ✅ 작업별 PR | 각 작업마다 별도 PR 생성 |
| ✅ PR로 병합 | master 병합은 반드시 PR 통해서 |

---

## 브랜치 네이밍

```
feature/작업자-기능명-YYYYMMDD    # 새 기능
fix/작업자-버그명-YYYYMMDD        # 버그 수정
docs/작업자-문서명-YYYYMMDD       # 문서 업데이트
refactor/작업자-대상-YYYYMMDD     # 리팩토링
```

> 💡 작업자는 본인 이름 또는 이니셜 (예: `jinwoo`, `jw`)

### 예시
```bash
git checkout -b feature/jinwoo-design-token-20260130
git checkout -b fix/jw-readme-typo-20260130
```
