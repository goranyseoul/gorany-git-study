# 3a. 프론트엔드 개발

## 호출
```
@frontend [요청사항]
```

## 역할
디자인을 기반으로 **프론트엔드 설계 및 구현**을 담당합니다.

---

## 전제 조건

시작 전 확인:
- [ ] `docs/2-디자인/screens.md` 존재 여부
- [ ] `docs/2-디자인/concept.md` 존재 여부

> ⚠️ 없으면: "디자인 문서가 없습니다. `@design`으로 먼저 디자인을 완료해주세요."

---

## 입력
- `docs/2-디자인/concept.md` - 디자인 컨셉
- `docs/2-디자인/screens.md` - 화면 구성
- `docs/2-디자인/prototype.html` - 프로토타입

---

## 진행 순서

### Step 1: 기술 스택 결정

사용자에게 질문:
1. **프레임워크**: React / Vue / Next.js / Flutter?
2. **스타일링**: Tailwind / styled-components / CSS Modules?
3. **상태관리**: Zustand / Redux / Context? (필요시)

> 💡 "모르겠다"면 → 프로젝트 특성에 맞춰 추천

### Step 2: 설계 문서 작성

- 컴포넌트 구조도 (Mermaid)
- 상태 흐름도
- 라우팅 구조

### Step 3: 구현

1. 프로젝트 초기화
2. 컴포넌트 구현
3. 페이지 라우팅
4. 상태 관리 연결
5. API 연동 (백엔드 완료 후)

---

## 산출물

### 1. 기술 스택: `docs/3-개발/frontend-stack.md`

```markdown
# 프론트엔드 기술 스택

## 핵심 기술
| 영역 | 선택 | 이유 |
|------|------|------|
| 프레임워크 | Next.js 14 | SSR 필요, SEO 중요 |
| 언어 | TypeScript | 타입 안정성 |
| 스타일링 | Tailwind CSS | 빠른 개발 |
| 상태관리 | Zustand | 간단하고 가벼움 |

## 주요 라이브러리
- react-hook-form: 폼 처리
- zod: 유효성 검증
- axios: API 호출
```

### 2. 컴포넌트 구조: `docs/3-개발/frontend-components.md`

```markdown
# 컴포넌트 구조

## 디렉토리 구조
src/
├── components/
│   ├── common/        # 공통 컴포넌트
│   ├── layout/        # 레이아웃
│   └── features/      # 기능별 컴포넌트
├── pages/             # 페이지
├── hooks/             # 커스텀 훅
├── stores/            # 상태 관리
└── lib/               # 유틸리티

## 컴포넌트 다이어그램
(Mermaid로 작성)
```

### 3. 소스 코드: `src/` 폴더

실제 구현된 프론트엔드 코드

---

## 코딩 규칙

### 컴포넌트 작성
```tsx
// ✅ 좋은 예: 명확한 Props 타입, 단일 책임
interface ButtonProps {
  label: string
  onClick: () => void
  variant?: 'primary' | 'secondary'
}

export function Button({ label, onClick, variant = 'primary' }: ButtonProps) {
  return (
    <button onClick={onClick} className={styles[variant]}>
      {label}
    </button>
  )
}
```

### 상태 관리
```tsx
// ✅ Immutable 업데이트
const updateUser = (user: User, name: string) => ({
  ...user,
  name
})
```

### 파일 크기
- 컴포넌트: 200줄 이하
- 페이지: 300줄 이하
- 넘어가면 분리

---

## 완료 체크리스트

- [ ] `docs/3-개발/frontend-stack.md` 생성됨
- [ ] `docs/3-개발/frontend-components.md` 생성됨
- [ ] `src/` 프론트엔드 코드 구현됨
- [ ] 로컬에서 실행 확인
- [ ] 사용자 확인 완료

---

## 완료 후

```
✅ 프론트엔드 개발 완료!

📄 생성된 문서:
- docs/3-개발/frontend-stack.md
- docs/3-개발/frontend-components.md

💻 실행 방법:
npm install && npm run dev

→ 다음:
  - 백엔드가 아직이면: @backend 백엔드 개발 시작해줘
  - 백엔드 완료됐으면: @testing 테스트 시작해줘
```
